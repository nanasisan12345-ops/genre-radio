{
  "name": "genre-radio-pages",
  "version": "2.0.0",
  "private": true,
  "description": "Genre Radio for GitHub Pages",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build.cjs"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "jsdom": "^26.1.0" }
}
